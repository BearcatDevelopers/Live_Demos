// we will be writing a function that will be run in our HTML document.

// the way to run JS in any webpage is to call on the document itself. The way
// to do this is to call "document" into the file.

document

// you'll notice the color changes, indicated it already has a predetermined role
// in JS.

// To manipulate specific elements in the document, you have to grab them. There
// are several ways to do that.

//What do these mean?
document.getElementsByClassName('className');
document.getElementsByName('name');
document.getElementsByTagName('tagName');
document.getElementById('id');


// usually we use these to get inputs from users and output the results back to them.
